# An example configuration file.
---
APPROACHES:
  my-oracle:  # used in constructing the experiment ID
    NAME: "oracle"
    FLAGS:
      offline_data_planning_timeout: 500.0
      timeout: 500.0
      plan_only_eval: True
ENVS:
  openining-packages-Pomaria_2_int:
    NAME: "behavior"
    FLAGS:
      behavior_scene_name: Pomaria_2_int
      behavior_task_list: "[opening_packages]"
      behavior_option_model_eval: True
FLAGS:  # general flags
  num_train_tasks: 0
  num_test_tasks: 10
START_SEED: 456
NUM_SEEDS: 3
